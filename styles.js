import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginVertical: 80,
  },

  mystyle: {
    backgroundColor: "red",
    fontSize: 30,
    fontWeight: "bold",
  },
});
